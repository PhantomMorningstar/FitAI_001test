# Back-end

This folder contains a simple Node.js server that serves the `front-end` files.

## Run
1. Install dependencies in the project root: `npm install`
2. Start the server from the root: `node back-end/server.js`
3. Open `http://localhost:3000`
